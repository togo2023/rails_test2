<%= link_to "← 戻る", :back %>
<%= link_to "編集", edit_room_path(@room.id) %>

<% if @room.image_room.attached? %>
  <p><%= image_tag(rails_blob_path(@room.image_room), size: "50x50",  alt: "ルーム画像") %></p>
<% else %>
  <p><%= image_tag("default_room.png", size: "50x50",  alt: "ルーム画像") %></p>
<% end %>

<%= @room.room_name %>
<%= "¥#{@room.money}~" %>
<%= @room.address %>
<%= @room.room_introduction %>
